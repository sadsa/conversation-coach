## Issue 01 — Session review state: data layer

Date: 2026-06-29

### Key decisions

- Extracted pure `deriveSessionReviewState(annotations: AnnotationReviewSummary[]) → SessionReviewState | null` into `lib/session-review-state.ts` for isolated unit testing.
- `AnnotationReviewSummary` carries `{ is_unhelpful: boolean; isSaved: boolean }` — "saved" means the annotation has a corresponding `practice_item` entry.
- Saved annotations take priority over dismissed ones: an annotation that is both `isSaved=true` and `is_unhelpful=true` counts toward the saved total, not as dismissed.
- `loadSessions` now fires 2 additional bulk queries after the sessions query (annotations + practice_items for all ready session IDs) — not N+1. Non-ready sessions get `review_state: null, saved_count: 0, due_count: 0`.
- `due_count` counts practice_items where `due IS NOT NULL AND due <= now` (no `reviewed` filter, consistent with PRD spec).
- Added `SessionReviewState` type alias and three new required fields (`review_state`, `saved_count`, `due_count`) to `SessionListItem` in `lib/types.ts`.
- Updated all test fixtures that construct `SessionListItem` directly to include the new required fields.
- `GET /api/sessions` test mock updated to table-name-aware mock supporting the new bulk queries.

### Files changed

- `lib/session-review-state.ts` (new) — pure derivation function
- `lib/types.ts` — `SessionReviewState` type + extended `SessionListItem`
- `lib/loaders.ts` — `loadSessions` extended with annotation/practice_item aggregation
- `__tests__/lib/session-review-state.test.ts` (new) — 10 unit tests covering all branches
- `__tests__/components/SessionList.test.tsx` — fixture updated
- `__tests__/components/ReviewClient.test.tsx` — fixture updated
- `__tests__/components/DashboardRecentSessions.test.tsx` — fixture updated
- `__tests__/api/sessions.test.ts` — GET /api/sessions mock updated

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` — no errors in source files
- `npm test -- __tests__/lib/session-review-state.test.ts` — 10/10 pass
- `npm run build` and component tests were already crashing on ARM64 before this issue (pre-existing jsdom/SWC native binary incompatibility in this container)

### Next-iteration notes

- Issue 02 (badge + counts) and Issue 03 (review inbox filter bar) are now unblocked — both depend on the `review_state` field now present on `SessionListItem`.
- Issue 02 will need to render badge states in `SessionList` rows using the new fields; the `Nothing kept` state needs an inline delete prompt.

## Issue 04 — `/study` route with dual loading modes

Date: 2026-06-29

### Key decisions

- Added `loadStudyItems(userId, opts: StudyMode)` to `lib/loaders.ts` handling three modes: session (bypasses SRS), srs (due<=now only), items (specific IDs with user_id enforcement).
- `LessonClient` extended with optional `onCompletionAction?: () => void` and `completionLabel?: string` props. When `onCompletionAction` is provided, the completion screen shows a button calling it (+ write-back) instead of the hardcoded `<Link href="/">`. Existing session-page behaviour unchanged.
- `StudyClient` wraps `LessonClient` in a `phase: 'lesson' | 'prompt'` state machine. Session mode: onExit transitions to the "Want to study more?" prompt screen. SRS/items modes: onExit navigates to `/vocabulary` via `router.push`.
- Auth guard is handled by existing middleware — no changes to `middleware.ts`.
- FSRS write-back reuses the existing `PATCH /api/practice-items/:id` fire-and-forget path inside `LessonClient.doWriteBackRef`.
- Used Python for i18n.ts edits to avoid the 32KB truncation bug in the Edit tool when files exceed that limit.

### Files changed

- `lib/loaders.ts` — added `StudyMode` type + `loadStudyItems` function
- `components/LessonClient.tsx` — added `onCompletionAction` and `completionLabel` props
- `lib/i18n.ts` — added `lesson.wantMoreHeading`, `lesson.wantMoreSub`, `lesson.wantMoreCta`, `lesson.backToVocabulary` (en + es)
- `app/study/page.tsx` (new) — server component; auth-guards, parses search params, calls `loadStudyItems`, redirects on empty
- `app/study/StudyClient.tsx` (new) — client component; phase state machine + "Want to study more?" prompt screen
- `__tests__/lib/loaders-study.test.ts` (new) — 11 unit tests covering all three modes (session, SRS, items)

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` (source files only) — no new errors
- `npm test -- __tests__/lib/loaders-study.test.ts` — 11/11 pass
- `npm run build` — pre-existing SIGSEGV crash on ARM64 container (unrelated, confirmed in memory)

### Next-iteration notes

- Issues 08 (due widget) and 09 (single-item study) are now unblocked — both depend on the `/study` route.
- Issue 02 "Study" button on inbox badge should navigate to `/study?session_id=<id>` — this route now exists.
- Single-item mode accepts comma-separated IDs (`item_ids=a,b,c`) — issue 09 needs to wire the per-row menu action.

## Issue 02 — Session item: state badge and counts

Date: 2026-06-29

### Key decisions

- Badge row (`hasBadgeRow = review_state !== null`) is rendered as a separate `<div>` sibling to the `<Link>` inside the card, avoiding nested interactive elements while keeping the card's visual coherence.
- `showDeletePrompt` local state initialises to `true` when `review_state === 'nothing_kept'`; Cancel hides it (component-local, resets on remount). The badge remains visible after dismissal.
- The Remove button in the nothing_kept prompt calls the same `onDelete` flow as the ⋮ menu — same 5s optimistic delete + Undo toast.
- Badge colours use existing design tokens: `bg-accent-chip/text-on-accent-chip` (partial), `bg-study-badge-bg/text-on-study-badge` (ready_to_study), `bg-surface-elevated/text-text-tertiary` (nothing_kept).
- Counts (`{n} saved · {n} due`) are hidden when both are 0 to avoid "0 saved · 0 due" noise (e.g. for partial sessions with no saves yet, or nothing_kept sessions).
- Used Python for i18n.ts edits to avoid the 32KB truncation bug.

### Files changed

- `lib/i18n.ts` — added `session.badge.partial`, `session.badge.readyToStudy`, `session.badge.nothingKept`, `session.action.review`, `session.action.study`, `session.counts`, `session.removePrompt`, `session.removeConfirm`, `session.removeCancel` (en + es)
- `components/SessionList.tsx` — added `BADGE_CLASSES` map, `hasBadgeRow`/`showDeletePrompt` state, badge+counts+action+delete-prompt UI
- `__tests__/components/SessionList.test.tsx` — added badge, counts, action button, and delete prompt test suites

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` (source files) — no new errors
- `npm test -- __tests__/lib/session-review-state.test.ts` — 10/10 pass
- Component tests crash on ARM64 container (pre-existing jsdom issue)

### Next-iteration notes

- Issue 03 (Review inbox filter bar) is the next architectural task with deps met — uses `review_state` to filter sessions client-side.
- Issues 05, 06, 07, 08, 09 are also unblocked.

## Issue 07 — Vocabulary page: filter bar

Date: 2026-06-29

### Key decisions

- Created shared `FilterBar` component (`components/FilterBar.tsx`) — generic pill-based filter UI reusable by Issue 03 (Review inbox).
- FilterBar API: `filterOptions[]`, `activeFilters[]`, `onFilterAdd`, `onFilterRemove`, `searchQuery`, `onSearchChange`. Labels are caller-supplied so the component needs no i18n knowledge.
- Filter state lives in `VocabularyClient` as `VocabularyFilterState { statusFilters, searchQuery }`. Derived `filteredItems` computed in `useMemo` before passing to `VocabularyList`.
- Pure `filterVocabularyItems(items, filter)` function in `lib/vocabulary-filter.ts` — testable in isolation. Status filters are ORed; text search ANDed on top.
- Text search matches `flashcard_back` OR `correction` (case-insensitive); falls back to `correction` when `flashcard_back` is null.
- Added `search` and `funnel` icons to `Icon.tsx` (stroke-based, consistent with existing icon style).
- Added i18n keys via Python (file is 74KB, above the Edit tool 32KB limit): `vocabulary.filter.searchPlaceholder`, `vocabulary.filter.button`, `vocabulary.filter.unstudied`, `vocabulary.filter.due`, `vocabulary.filter.studied` (en + es).
- No existing sort UI in `VocabularyClient` to preserve — the loader accepts a `sort` param but it was never surfaced in the UI.

### Files changed

- `lib/vocabulary-filter.ts` (new) — pure filter transform
- `components/FilterBar.tsx` (new) — shared filter bar component
- `components/VocabularyClient.tsx` — filter state, FilterBar wiring, filteredItems
- `components/Icon.tsx` — added `search` and `funnel` icons
- `lib/i18n.ts` — added filter keys (en + es)
- `__tests__/lib/vocabulary-filter.test.ts` (new) — 13 unit tests
- `__tests__/components/FilterBar.test.tsx` (new) — component tests (can't run in ARM64 jsdom)

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` (source files) — no new errors in new files
- `npm test -- __tests__/lib/vocabulary-filter.test.ts` — 13/13 pass
- Component tests and `npm run build` crash pre-existing on ARM64 container

### Next-iteration notes

- Issue 03 (Review inbox filter bar) can now reuse `FilterBar` — pass `review_state`-based options and a `filterSessions` pure function.
- The `FilterBar` dropdown hides options that are already active; when all 3 status options are active, the trigger button hides entirely — correct UX.

## Issue 03 — Review inbox: filter bar

Date: 2026-06-29

### Key decisions

- Reused shared `FilterBar` component built in Issue 07 — no new component needed.
- Pure `filterSessions(sessions, filter)` in `lib/session-filter.ts` — same pattern as `filterVocabularyItems`. Status filters ORed; text search ANDed on top; searches session `title`.
- `SessionStatusFilter = 'partial' | 'ready_to_study'` — `'nothing_kept'` is intentionally excluded (those sessions are typically prompted for deletion, not studied).
- Sessions with `review_state: null` are excluded when any status filter is active (consistent with PRD: null means no annotations yet, which doesn't fit "In progress" or "Ready to study").
- Filter state lives in `ReviewClient` as `SessionFilterState`; `filteredTerminalSessions` computed via `useMemo` before passing to `DashboardRecentSessions`.
- i18n keys added via Python (file exceeds 32KB Edit tool limit): `review.filter.searchPlaceholder`, `review.filter.button`, `review.filter.inProgress`, `review.filter.readyToStudy` (en + es).
- CONTEXT.md updated: added Session Review State domain term; updated Study definition for two launch modes; updated Dismiss terminology.

### Files changed

- `lib/session-filter.ts` (new) — pure filter transform
- `__tests__/lib/session-filter.test.ts` (new) — 12 unit tests
- `components/ReviewClient.tsx` — filter state, FilterBar wiring, filteredTerminalSessions
- `lib/i18n.ts` — added filter keys (en + es)
- `__tests__/components/ReviewClient.test.tsx` — added filter bar test suite (6 tests)
- `CONTEXT.md` — domain term updates (Session Review State, Dismiss, Study modes)

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` (source files) — no new errors in changed files
- `npm test -- __tests__/lib/session-filter.test.ts` — 12/12 pass
- Component tests and `npm run build` crash pre-existing on ARM64 container

### Next-iteration notes

- Issues 05, 06, 08, 09 remain unblocked — pick highest priority next.
- Issue 08 (due widget) and 09 (single-item study) both depend on the `/study` route (04, done).
- Issue 05 (StudyPrompt) and 06 (VocabularyRow rename) have no dependencies.

## Issue 06 — Vocabulary row: remove English sentence and rename component

Date: 2026-06-29

### Key decisions

- Created `VocabularyRow` as a new component (`components/VocabularyRow.tsx`) that renders only `flashcard_back` — no `flashcard_front` (English sentence). `FlashcardRow` is kept intact for `ReviewCompletionScreen.tsx` which still needs both sides.
- Render guard in `WriteList` changed from `item.flashcard_front && item.flashcard_back` to `item.flashcard_back` — an item with only a back (Spanish) sentence can now render correctly; in practice both fields are always set together by Claude.
- `testId` prefix changed from `flashcard-row-` to `vocabulary-row-` to match the renamed component.
- Styling is identical to the back-row of `FlashcardRow`: Source Serif 4, correction-green bracketed span, muted variant for reviewed items.

### Files changed

- `components/VocabularyRow.tsx` (new) — list-row component showing only `flashcard_back`
- `components/WriteList.tsx` — import changed from `FlashcardRow` to `VocabularyRow`; render condition loosened to `item.flashcard_back`; testId updated
- `__tests__/components/VocabularyRow.test.tsx` (new) — 6 tests: back renders, front element absent, display font, styled span, no-brackets fallback
- `__tests__/components/WriteList.test.tsx` — updated FlashcardRow test: checks `vocabulary-row-*` testId, asserts back content shown, asserts front text not in DOM

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` — no errors in new/changed source files
- `npm run build` — pre-existing SIGSEGV on ARM64 container (confirmed pre-existing with clean main)
- Component tests cannot run in ARM64 jsdom environment (pre-existing)

### Next-iteration notes

- Issues 05, 08, 09 remain. Issue 08 (due widget) is likely next — architectural work, depends on loadVocabularyItems exposing dueCount to VocabularyClient.

## Issue 08 — Vocabulary page: due-count widget

Date: 2026-06-29

### Key decisions

- Created standalone `DueWidget` component (`components/DueWidget.tsx`) — renders a compact banner with due count text and a "Study now" link to `/study`. Returns `null` when `dueCount === 0` so it takes no DOM space.
- Replaced the previous inline `<p>` badge in `VocabularyClient.tsx` with `<DueWidget>`. Widget is placed below the `FilterBar` and above `VocabularyList` (per issue spec: "above the Vocabulary list, below the filter bar").
- Added `vocabulary.studyNow` i18n key (`'Study now'` / `'Estudiar ahora'`) via Python to avoid the 32KB Edit tool truncation.
- The existing `dueCount` from `loadPracticeItems` and the server component was already correctly wired through — no changes needed to `lib/loaders.ts` or `app/vocabulary/page.tsx`.

### Files changed

- `components/DueWidget.tsx` (new) — standalone reusable due-count banner
- `components/VocabularyClient.tsx` — imports `DueWidget`, removes inline badge, places widget below FilterBar
- `lib/i18n.ts` — added `vocabulary.studyNow` (en + es)
- `__tests__/components/DueWidget.test.tsx` (new) — 5 tests: renders when due > 0, nothing when 0, count shown, href is /study, label text

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` — no errors in new/changed source files
- Component tests cannot run in ARM64 jsdom environment (pre-existing)

### Next-iteration notes

- Issues 05 and 09 remain. Issue 09 (single-item study) is the next architectural task.
- In a future iteration, `DueWidget` can be imported directly into the home page (`app/page.tsx`) as the acceptance criteria note.

## Issue 09 — Vocabulary row: single-item study action

Date: 2026-06-29

### Key decisions

- Extended `RowAction` interface with optional `href?: string` field. When `href` is present, the menu item renders as a `<Link>` element (testable via `href` attribute) instead of a `<button>`. Existing `onSelect`-only callers (`SessionList.tsx`) are unaffected — `onSelect` changed from required to optional, TypeScript confirms no breakage.
- Added "Study" action as the first item in `WriteRow`'s action list (before Delete) — consistent positioning as a non-destructive primary action above the destructive one.
- Added `writeList.menuStudy` i18n key (`'Study'` / `'Estudiar'`) via Python to avoid the 32KB Edit tool truncation.
- `testId` for the Study link: `row-study-${item.id}` — mirrors the `row-delete-${item.id}` pattern for consistency.

### Files changed

- `components/RowActionsMenu.tsx` — added `href?: string` and `onSelect?: () => void` (optional) to `RowAction`; render branch for link-type actions using `<Link>`
- `components/WriteList.tsx` — added Study action to `WriteRow` actions array with `href: \`/study?item_ids=${item.id}\``
- `lib/i18n.ts` — added `writeList.menuStudy` (en + es)
- `__tests__/components/WriteList.test.tsx` — updated menu test to assert Study action present; added test asserting href is `/study?item_ids=<id>`

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` — no errors in new/changed source files
- Confirmed `SessionList.tsx` backward-compatible (uses `onSelect` only, no `href`)
- Component tests cannot run in ARM64 jsdom environment (pre-existing)

### Next-iteration notes

- Issue 05 (StudyPrompt empty state) is the last remaining issue.

## Issue 05 — StudyPrompt: remove "Save a phrase" and update empty state

Date: 2026-06-29

### Key decisions

- Removed `onSavePhrase` prop from `StudyPrompt` entirely — no more scroll-to-first-annotation behavior. The prop is gone from the interface and all callers.
- Added `reviewState?: SessionReviewState | null` prop to `StudyPrompt`. When `count = 0`:
  - `nothing_kept` → shows "Nothing saved — you can remove this session"
  - Anything else (partial, null, undefined) → shows "Keep going — save anything worth practising" as the safe default
- Status text is now always shown (no longer conditionally renders only when `count > 0`). The motion key is `${count}-${reviewState}` so the animation fires on state changes too.
- `TranscriptView` threaded `reviewState?: SessionReviewState | null` through to `StudyPrompt` (removed `onSavePhrase` from the render call).
- `TranscriptClient` computes `reviewState` via `useMemo` + `deriveSessionReviewState` from `lib/session-review-state.ts`, reacting to `detail.annotations`, `addedAnnotations`, `unhelpfulAnnotations`.
- i18n keys added via Python: `transcript.emptyPartial` and `transcript.emptyNothingKept` (en + es).

### Files changed

- `components/StudyPrompt.tsx` — removed `onSavePhrase` prop, added `reviewState` prop, always renders status text, Study button only shows when `count > 0`
- `components/TranscriptView.tsx` — removed `onSavePhrase` prop/usage, added `reviewState` prop, passes to `StudyPrompt`
- `components/TranscriptClient.tsx` — added `useMemo` + `deriveSessionReviewState` import; computes `reviewState`; passes to `TranscriptView`
- `lib/i18n.ts` — added `transcript.emptyPartial` and `transcript.emptyNothingKept` (en + es)
- `__tests__/components/StudyPrompt.test.tsx` — rewrote tests: removed `onSavePhrase` from defaultProps, asserted "Save a phrase" absent, added partial/nothing_kept copy tests

### Verification

- `npm run lint` — clean
- `npx tsc --noEmit` — no errors in new/changed source files (pre-existing errors in test fixtures unaffected)
- Component tests cannot run in ARM64 jsdom environment (pre-existing)

### Next-iteration notes

- All 9 issues are now done. No further work required for this feature set.
